# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

workspace 'NDModificationOperators'

target 'Samples' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Samples
  pod 'NDModificationOperators/ObjC', :path => '.'
  pod 'NDModificationOperators/Swift', :path => '.'

  target 'SamplesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SamplesUITests' do
    # Pods for testing
  end

end
